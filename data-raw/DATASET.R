#
# ## code to prepare `DATASET` dataset goes here
#
#
#
# devtools::load_all()
#
# b <- hdbase::hdbase(list(cars = cars, iris = iris),
#             name = "Cars and Iris"
#             )
# base_samples <- list(cars_iris = b)
#
# usethis::use_data(base_samples, overwrite = TRUE, internal = TRUE)
